## 1. Mobile settings surface layout

- [x] 1.1 Update the mobile settings container in `src/App.tsx` so it uses a wider, sheet-like presentation on narrow screens while keeping the desktop popover behavior
- [x] 1.2 Adjust settings panel spacing, status row, and field layout so the mobile content is easier to scan and interact with

## 2. Verification

- [ ] 2.1 Manually verify the settings panel at mobile and desktop widths, including long values for host, port, location, and status text
- [x] 2.2 Run `npm run lint` and `npm run build` after the layout fix is implemented
